@model IEnumerable<Finances.WebApp.Models.CategoriaViewModel>

@{
    ViewBag.Title = "Categorias";
}

<h2>Categorias</h2>

<p><a asp-action="Create">Criar Nova</a></p>
<br />

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Editar</a> |
                <a asp-action="Delete" asp-route-id="@item.ID" onclick="return confirm('Deseja apagar a categoria @item.ID ?');">Apagar</a>
            </td>
        </tr>
    }

</table>
<br />